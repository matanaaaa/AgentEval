# Agent 认证配置模板
# 复制此文件为 credentials.yaml 并填入真实信息
# credentials.yaml 已加入 .gitignore，不会被提交

# CRM 登录信息（自动登录）
base_url: "https://crm-cd.xiaoshouyi.com"
login_url: "https://login-cd.xiaoshouyi.com"
username: "your_email@example.com"
password: "your_password"
tenant: "租户名称"

# 或者直接填 Cookie（从浏览器 DevTools 复制，跳过自动登录）
# cookie: "x-ienterprise-passport=xxx; x-ienterprise-tenant=xxx"

# AI Testing Agent（testgen 用例生成流水线）用的 LLM key。
# 也可用环境变量 LLM_API_KEY 覆盖；base_url/model 用 LLM_BASE_URL / LLM_MODEL。
# llm_api_key: "sk-xxxx"
